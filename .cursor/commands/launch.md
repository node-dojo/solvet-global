Check if the following servers are running:
- Main website server at http://localhost:3001/
- Co-aug dashboard at http://localhost:5551/

If either server is not running, launch them at those addresses.  
Refresh any servers that are already running.